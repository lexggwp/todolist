export const taskReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'XXX':
            return state;
        case 'YYY':
            return state;
        default:
            return state;
    }
}